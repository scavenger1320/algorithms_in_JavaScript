'use strict';

// create a new singly linked list class that will be exported to other files to use
module.exports = function SLL(){
	this.head = null; // a new list simply has a head so when first created it will be empty until we add some nodes to it
}